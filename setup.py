from setuptools import setup

setup(
    name="addiction_simulation",
    version="0.0.1",
    install_requires=["gym"],
)

setup(
    name="rl_algorithms",
    version="0.0.1",
    install_requires=["gym", "numpy"],
)